from django.urls import path

# Routing for this app is provided via DRF's router in `backend/preppr/urls.py`.
# These placeholders are intentionally empty to avoid duplicate or stale routes.

app_name = "user_app"

urlpatterns = []
